import React from 'react';
import { Divider } from 'react-native-elements';

import { primaryColor } from '../../styles';

const CallDivider = () => {
  return <Divider style={{ backgroundColor: primaryColor }} />;
};

export default CallDivider;
